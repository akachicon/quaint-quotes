extends layout-basic

block content
  if (regSuccess)
    p(
      class='alert alert-success body-alert'
      role='alert') #{ regSuccess }
  if (regFailure)
    p(
      class='alert alert-danger body-alert'
      role='alert') #{ regFailure }

  h2.pl-4 Your are at home now. Welcome!
